!!! 5
html
  head
    title 1/2 - もっとひっついてたいのさ
    script(src='/socket.io/socket.io.js')
    script
      var socket = new io.Socket();
      socket.on('connect', function() {
        socket.send({message: "hi"});
        socket.on('message', function(message) {
          console.log(message);
        });
      });
      socket.connect();

  body
    != body
